[package]
name = "hive-sense-fw"
version = "1.2.0"
authors = ["Pierrick Curt <pierrickcurt@gmail.com>"]
edition = "2021"
license = "MIT OR Apache-2.0"

[dependencies]
esp-backtrace = { version = "0.13.0", features = [
    "esp32s3",
    "panic-handler",
    "println",
] }

esp-hal = { version = "0.19.0", features = ["async", "esp32s3", "embedded-hal-02"] }
esp-println = { version = "0.10.0", features = ["esp32s3", "log"] }
log = { version = "0.4.21" }

lora-phy = { features = ["lorawan-radio"], version = "3.0.1" }
lorawan-device = { features = ["embassy-time", "default-crypto"], version = "0.12.2"}
embassy-executor =  { version = "0.5.0" }
embassy-time =  { version = "0.3.0" }
embedded-hal-bus = { version = "0.2.0", features = ["async"] }
loadcell = "0.2.0"
esp-hal-embassy = { version = "0.2.0", features = ["esp32s3", "integrated-timers"] }
esp-wifi = { version = "0.7.1", features = [
    "esp32s3",
    "wifi-default",
    "utils",
    "phy-enable-usb",
] }
heapless = { version = "0.8.0", default-features = false }
embedded-cli = "0.2.1"
embedded-io = "0.6.1"
ufmt = "0.2.0"
esp-storage = { version = "0.3.0", features = ["esp32s3"] }
embedded-storage = "0.3.1"
embassy-sync = "0.6.0"
static_cell = { version = "2.1.0", features = ["nightly"] }
#lis2dh12 = "0.7.0"
#lis2dw12 = "0.1.3"
embassy-embedded-hal = "0.2.0"
defmt = "0.3.8"
#embedded-hal-async = "1.0.0"

[profile.dev]
# Rust debug is too slow. 
# For debug builds always builds with some optimization
opt-level = "s"

[profile.dev.package.esp-wifi]
opt-level = 3

[profile.release]
codegen-units = 1 # LLVM can perform better optimizations using a single thread
debug = 2
debug-assertions = false
incremental = false
lto = 'fat'
opt-level = 's'
overflow-checks = false

